---
bootstrap:
  - tests/bootstrap.php
schema:
  paths:
    - tests/schema
data_sources:
  default:
    # dsn: 'sqlite:tests.db'
    dsn: 'pgsql:dbname=lazy_test'
    # dsn: 'sqlite::memory:'
  mysql:
    dsn: 'mysql:host=localhost;dbname=test'
    user: root
    pass: 123123
