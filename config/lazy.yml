---
bootstrap:
  - tests/bootstrap.php
schema:
  paths:
    - tests/schema
data_sources:
  default:
    dsn: 'sqlite:tests.db'
    # dsn: 'sqlite::memory:'
  slave:
    dsn: 'mysql:host=localhost;dbname=test'
    user: root
    pass: 123123
