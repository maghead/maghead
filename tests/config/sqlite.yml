---
configServer: "mongodb://127.0.0.1"
cli:
  bootstrap: tests/bootstrap.php
schema:
  auto_id: true
  base_model: \Maghead\Runtime\BaseModel
  base_collection: \Maghead\Runtime\BaseCollection
  paths:
    - tests
instance:
  local:
    dsn: 'sqlite::memory:'
databases:
  master:
    dsn: 'sqlite::memory:'
    query_options: { quote_table: true }
  node1:
    dsn: 'sqlite::memory:'
    query_options: { quote_table: true }
  node2:
    dsn: 'sqlite::memory:'
    query_options: { quote_table: true }
  node3:
    dsn: 'sqlite::memory:'
    query_options: { quote_table: true }
